<%= render partial: 'shared/model_error_messages', locals: {model: @cuisine} %>
<%= form_for @cuisine do |f| %>
  <%= f.label :name, 'Nome' %>
  <%= f.text_field :name %>
  <%= f.submit 'Enviar' %>
<% end %>